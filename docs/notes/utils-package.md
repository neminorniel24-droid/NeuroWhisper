# utils/ package

- config_loader.py / config_models.py / config_validation.py — typed, validated config
- logging.py — central logging setup
- paths.py — project path helpers
- reproducibility.py — random seed control for repeatable results
