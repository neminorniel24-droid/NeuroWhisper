# File Formats Used

.tar.bz2 — compressed raw archive per subject
.set/.fdt — EEGLAB raw EEG format (header + data pair)
.fif — MNE-Python filtered EEG format
.mat — MATLAB feature files bundled with original dataset (unused directly)
