# Windowing Notes

length_factor and overlap in config.yaml control the sliding window used
to segment each "thinking" epoch into fixed-length analysis windows before
feature extraction. Smaller windows = more samples but noisier features.
