# experiments/ package

- registry.py — tracks distinct experiment runs
- results.py — standardizes result output (accuracy, confusion matrix, config used)

Goal: every experiment run is reproducible and comparable.
