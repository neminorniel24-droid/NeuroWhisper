# config.yaml Field Reference

karaone.epoch_type: which epoch to process (thinking = imagined speech)
karaone.raw_data_dir: location of extracted raw EEG
karaone.filtered_data_dir: where filtered .fif files are saved
karaone.features_dir: where extracted feature vectors are saved
karaone.length_factor / overlap: sliding window parameters for epoching
