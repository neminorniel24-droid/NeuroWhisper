# data/ package

- discovery.py — locates raw subject files on disk
- inventory.py — builds a list of what's present vs expected
- integrity.py — validates files aren't corrupted/incomplete
- manifest.py — generates a manifest of the dataset state
- validation.py — schema/shape checks on loaded data
