# Testing Strategy

- Unit tests for pure functions (scaling, selection, config validation)
- Sanity tests for project structure (docs, license, gitignore)
- No tests depend on the actual downloaded dataset (kept fast + portable)
