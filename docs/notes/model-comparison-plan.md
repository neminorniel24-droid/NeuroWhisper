# Model Comparison Plan

Stage 1: SVM baseline (classical, fast, interpretable)
Stage 2: Random Forest (classical, ensemble comparison)
Stage 3: CNN or LSTM (deep learning, temporal patterns)
Stage 4 (stretch): quantum-classical hybrid classifier

Each stage logged as a separate experiment via experiments/registry.py
